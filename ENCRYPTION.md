# Encryption

Color history is stored as JSON in UserDefaults (not encrypted), matching other Buddy utilities. See shared-buddy ENCRYPTION.md for App Store questionnaire guidance.

ITSAppUsesNonExemptEncryption = false.
